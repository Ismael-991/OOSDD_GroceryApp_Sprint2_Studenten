name: MAUI Unit Tests

on:
  push:
    branches:
      - main
      - infra/runner-toevoegen
      - develop
      - 'feature/**'
  pull_request:
    branches:
      - main
      - develop

jobs:
  test:
    runs-on: macos-latest
    steps:
      - name: Haal de repository op
        uses: actions/checkout@v4s

      - name: Installeer .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Install MAUI iOS workload
        run: dotnet workload install ios

      - name: Herstel afhankelijkheden
        run: dotnet restore Grocery.App/Grocery.App.csproj

      - name: Bouw de applicatie
        run: dotnet build Grocery.App/Grocery.App.csproj -c Release

      - name: Voer unit tests uit
        run: dotnet test --configuration Release --no-restore --no-build --verbosity normal
